# Provisioned so the stack is useful on first boot rather than after a manual
# datasource setup step.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
